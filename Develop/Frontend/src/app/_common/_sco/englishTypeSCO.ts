import { BaseSCO } from "./baseSCO";
import { SearchText } from "./core_sco";

export class EnglishTypeSCO extends BaseSCO {
  typeClass: SearchText;
  typeCode: SearchText;
  typeName: SearchText;
  description: SearchText;
}
